levelNScene = new Phaser.Scene('Level_N');
levelN.preload = function(){
	//load map, towers, and wave data from files in assets directory
}

levelN.create = function(){
	//create map, towers, and waves with respect to this scene and phaser, initialize values for first wave/
	//initial setup for level
}

levelN.create = function(){
	//level update based on player input and waves
}